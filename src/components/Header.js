import React from 'react';

function Header() {
    return (
      <header>
        <h1>ShiningPath</h1>
        <button className="btn-login">로그인</button>
      </header>
    );
}

export default Header;
